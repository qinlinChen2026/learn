# 2026-09-11 学习日记

## 今天学了什么（速览）

| 主题 | 一句话记住 | 分类 |
|---|---|---|
| **大模型是什么** | 用海量文本喂出来的"概率续写机"——能理解/生成，但不是真"懂" | 概念 |
| **cd 命令** | change directory，切换目录；`cd` 不带参数直接回家，`cd -` 回上一站 | 命令行 |
| **ls 命令** | list，列出目录内容；`-l` 看权限大小、`-a` 看隐藏、`-h` 看人类可读大小 | 命令行 |
| **mv 命令** | move，移动+改名；最后参数是目录就移动、是文件名就改名；默认静默覆盖，加 `-i`/`-n` 防丢 | 命令行 |
| **mkdir 命令** | make directory，新建目录；建多层嵌套必须加 `-p` | 命令行 |
| **touch 命令** | 建空文件 / 刷新修改时间；`-c` 文件不存在就不建，避免误建 | 命令行 |

## 今天的学习经历

- **晚上 22:36** 回顾今天学到的三个点，准备记成日记：一个概念（大模型）、两个命令（cd / ls）。
- 概念部分：把"大模型"从比喻讲到本质——参数量、预训练数据、自回归续写，以及它"会猜词但不会真懂"的边界。
- 命令部分：按规矩先在 `/tmp` 真跑了一遍，确认了 `cd` 无参回家、`cd -` 回上一站；`ls` 默认不显隐藏、`-a` 含 `.`/`..`、`-G` 才是 macOS 上色开关。
- 后来又补了 `mv`：实测改名 / 移进目录 / 多文件一起移入，重点确认它**默认会静默覆盖**（防丢用 `-i` 或 `-n`），以及 macOS **不支持** GNU 的 `-b` 备份（写了直接报 `illegal option -- b`）。
- 再补 `mkdir` / `touch`：实测 `mkdir -p` 一次建嵌套、缺 `-p` 建多层报 `No such file or directory`；`touch` 建 0 字节空文件并刷新修改时间，`-c` 不存在就不建（避免误建），也能 `touch` 目录刷其时间。

> 今天的核心体会：**命令要亲手跑，概念要分清"能做什么"和"本质是什么"**。大模型很会"像人一样说"，但它只是在预测下一个词。

---

## 详细笔记

### 1. 大模型（LLM）到底是什么

> 分类：概念 / AI 通识

#### 一句话

大模型（Large Language Model，大语言模型）是用**海量文本**训练出来的**概率续写机**：给它一串词，它预测"下一个词最可能是什么"，一个一个接下去，就形成了回答。

#### 三个关键词

| 词 | 大白话 |
|---|---|
| **参数（Parameters）** | 模型内部的可调数值，几十亿到上万亿个。参数越多，能记住的模式越细。常听到的 "7B / 70B" 就是指参数量（B = 十亿） |
| **预训练（Pre-training）** | 把全网/书籍/代码等海量文本喂进去，让它"学语言规律"。这一步烧钱、费算力 |
| **自回归生成** | 它一次只生成一个 token（词片段），把刚生成的接在尾巴上，再生成下一个——所以叫"续写" |

#### 它能做 / 不能做

- ✅ 写文章、改代码、翻译、总结、聊天——本质是"基于见过的大量文本做概率续写"
- ✅ 很多任务靠"提示词（Prompt）"引导，不需要重新训练
- ⚠️ **它不"理解"世界**，只是见过足够多相似文本后学会了"像人那样接话"
- ⚠️ 会一本正经地胡说（幻觉）、会过时（知识有截止日期）、不会替你核实事实

> 一句话记忆：大模型是"读过很多书、很会接话"的概率机器，不是"真懂世界"的大脑。把它当能力强但需要你把关的助手。

#### 大模型 与 Agent：谁是谁（一个比喻）

光说"概率续写机"还是抽象。换个更好懂的：

- **大模型 = 大哥**：脑子好使，会分析、会出主意，但**只能动嘴，不能直接动手**——它自己没法读写文件、跑命令、上网。
- **Agent = 给大哥配的一整套"外挂身体 + 秘书团队 + 工具箱"**：对讲机（收你的指令）、记事本（记忆）、手脚（执行）、流程（编排），还能调用各种工具。

**一次任务的完整循环（也是现在这个笔记空间里实际发生的事）**：

1. **你（老板）** 对 Agent 系统说：我要做这件事。
2. Agent 把目标递给里面的核心——**大哥**。大哥一通分析：先这样、再这样。
3. Agent 系统照着去**跑腿、调工具、执行**；中间结果再拿回来给大哥看。
4. 大哥**继续分析、调整方案**，Agent 再执行。如此循环，直到任务完成。
5. 最后 Agent 系统把结果**整理好**，反馈给你。

> 一句话：**大哥负责想，Agent 负责让大哥"能想也能做"。Agent 不是独立的人，而是围着大哥搭起来的执行系统。**

#### 和我们这个笔记空间的关系

你正在用的 WorkBuddy，背后就是大模型在驱动——它读你的文件、跑命令、写笔记，本质都是"续写 + 工具调用"。今天学的 `cd`/`ls`，都是它实际在帮你做的事。

---

### 2. cd —— 切换目录

> 分类：命令行 / Linux & macOS

#### 一句话

`cd` = **change directory**，用来**切换当前所在的目录**。命令行里"你在哪"很重要，所有相对路径都从当前目录算起。

#### 几种用法（实测）

```bash
cd 文档/          # 进入子目录
cd ..            # 回到上一级目录
cd               # 不带参数 → 直接回家（~，即 /Users/qinglin）
cd -             # 回到"上一次"所在的目录（在两地之间来回跳很方便）
cd ~             # 回家，等价于上面那个 cd
cd /tmp/lltest   # 绝对路径，从根目录写起，去哪都行
```

**实测结果**：

```
/tmp/lltest        ← cd /tmp/lltest
/Users/qinglin     ← cd（无参，回家）
/Users/qinglin     ← cd -（回到上一次目录）
```

#### 两个最常用技巧

1. **`cd -`**：在 A、B 两个目录间反复横跳时，比敲两遍路径省事。
2. **`cd` 回家**：迷路了直接 `cd` 回 `~`，再重新找。

#### 易错点

- 目录名有**空格**要加引号或转义：`cd "我的 文档"` 或 `cd 我的\ 文档`。
- 路径拼错会报 `No such file or directory`——先 `ls` 看一眼再 `cd`。
- `cd` 只换"当前目录"，**不移动任何文件**（和 `mv` 完全不同）。

> 一句话记忆：`cd` 是"挪自己的位置"，`mv` 是"挪文件的位置"。

---

### 3. ls —— 列出目录内容

> 分类：命令行 / Linux & macOS

#### 一句话

`ls` = **list**，列出**当前目录（或指定目录）里有什么**。配合选项能看权限、大小、隐藏文件。

#### 常用选项（实测）

```bash
ls            # 默认：只列名字，不显隐藏文件
ls -a         # 连隐藏文件（. 开头）一起列，含 . 和 ..
ls -l         # 长格式：权限、所有者、大小、时间
ls -lh        # 在 -l 基础上把字节变成 K/M/G（human readable）
ls -G         # 上色（macOS / BSD 版）
ls 文档/      # 列指定目录，不用先 cd 进去
```

**实测结果**：

```
a.txt  sub                  ← ls（默认，不含 .hidden）
.  ..  .hidden  a.txt  sub  ← ls -a（含隐藏）
-rw-r--r--@ 1 qinglin wheel 0B ... a.txt   ← ls -lh（人类可读大小）
```

#### `-l` 长格式每列啥意思

```
-rw-r--r--@  1  qinglin  wheel   0B  Sep 11  a.txt
│           │  │        │        │      │
权限        链接数 所有者   组    大小   修改时间  名字
```

- 权限第一位：`-` 普通文件，`d` 目录。后面 9 位分三组（自己/同组/其他人）的 读(r)写(w)执行(x)。
- 大小带 `-h` 才好看：0B / 96B / 4.0K 这种。

#### macOS（BSD）vs GNU/Linux 差异

| 需求 | macOS / BSD | GNU/Linux |
|---|---|---|
| 彩色输出 | `ls -G` | `ls --color=auto` |
| 排序按时间 | `ls -t` | `ls -t`（相同） |

⚠️ macOS 上写 `--color=auto` **不会报错，只是被忽略**（不会上色）。写跨平台脚本用 `-G` 更稳，或在 `.zshrc` 里 `alias ls='ls -G'`。

#### 常用组合

| 组合 | 用途 |
|---|---|
| `ls -la` | 看全部（含隐藏）+ 详情，排查配置最常用 |
| `ls -lhS` | 按大小排，找大文件 |
| `ls -lt` | 按时间排，看最近改了啥 |

> 一句话记忆：`ls` 是"看一眼这屋有啥"；想看全 `+ 详情` 就 `ls -la`。

---

### 4. mv —— 移动与改名

> 分类：命令行 / Linux & macOS

#### 一句话

`mv` = **move**，既能**移动**文件/目录，也能给它们**改名**。规则：`mv 源 ... 目标`，**最后一个参数是目录就移动进去，是文件名就改名**。

#### 常用用法（实测）

```bash
mv old.md new.md        # 改名
mv file.md notes/       # 移进 notes 目录
mv a.md b.md notes/     # 多个文件一起移进 notes/
mv *.txt backup/        # 通配符：所有 .txt 移走
```

目录同样适用：`mv old_folder new_folder` 就是给文件夹改名。

#### ⚠️ 最关键的坑：默认静默覆盖

`mv` 不会问你，目标已存在就**直接覆盖**，且不可恢复：

```
mv a.txt b.txt    # 若 b.txt 已有内容 → 被 a.txt 静默覆盖，无提示！
```

**防手滑参数（macOS 实测均支持）**：

| 参数 | 作用 | 实测 |
|---|---|---|
| `mv -i` | 覆盖前问 `overwrite? (y/n [n])` | ✅ 喂 `n` 成功拦下 |
| `mv -n` | 目标存在就跳过，绝不覆盖 | ✅ `keep.txt` 内容保持原样 |
| `mv -v` | 显示"从→到"过程 | ✅ 输出 `sub/x.txt -> ./x.txt` |

> 新手建议：不确定就加 `-i` 或 `-n`。

#### macOS（BSD）vs GNU/Linux 差异

| 需求 | macOS / BSD | GNU/Linux |
|---|---|---|
| 覆盖前备份 | ❌ 不支持 `-b`（报错 `illegal option -- b`） | ✅ `mv -b` 备份成 `x.txt~` |
| 不覆盖 | `mv -n` | `mv -n`（相同） |
| 交互确认 | `mv -i` | `mv -i`（相同） |

⚠️ macOS 上写 `mv -b` 会直接报错。想要备份效果，Mac 上只能先 `cp` 再 `mv`。

> 一句话记忆：`mv` 是"挪文件的位置"；默认会覆盖，保命加 `-i` 或 `-n`。

---

### 5. mkdir —— 新建目录

> 分类：命令行 / Linux & macOS

#### 一句话

`mkdir` = **make directory**，用来**新建目录**。

#### 常用用法（实测）

```bash
mkdir one            # 建一个
mkdir a b c          # 一次建多个
mkdir -p x/y/z       # 一次建嵌套多层（自动补中间的父目录）
mkdir -v log         # 建的时候回显名字（macOS 实测支持，输出 log）
```

#### ⚠️ 关键坑：建多层必须加 `-p`

```
mkdir x/y            # 若 x 不存在 → 报错：mkdir: x: No such file or directory
mkdir -p x/y         # ✅ 自动建好 x 再建 y
```

#### macOS vs GNU

| 需求 | 写法（两边通用） |
|---|---|
| 建单层 | `mkdir 名` |
| 建多层 | `mkdir -p 父/子/孙` |
| 回显过程 | `mkdir -v` |

> 一句话记忆：`mkdir` 建目录；要一次建一串层级，记得 `-p`。

---

### 6. touch —— 建空文件 / 刷新时间

> 分类：命令行 / Linux & macOS

#### 一句话

`touch` 有两个用途：**新建空文件**，或**刷新已有文件/目录的修改时间**。

#### 常用用法（实测）

```bash
touch note.txt            # 新建空文件（0 字节）
touch a.md b.md           # 一次建多个
touch 已有.txt             # 不改动内容，只把"修改时间"刷成现在
touch -c 可能不存在.txt    # -c：文件不存在就不建（避免误建）
touch 目录名               # 也能刷新目录的修改时间
```

#### 实测确认

- 新建空文件：`touch note.txt` → `ls -l` 看到大小 `0`，时间就是此刻。
- 刷新时间：对已有文件再 `touch` 一次，修改时间从 `23:50:43` 变 `23:50:44`。
- `-c` 安全：对不存在的文件 `touch -c xxx`，不会创建它（`ls` 报 No such file）。
- 对目录 `touch one`，它的修改时间同样被刷新。

#### ⚠️ 易混点

`touch` **不往文件里写任何内容**，只是建个空壳或刷时间戳。要写内容用 `echo 文本 > 文件` 或编辑器。

> 一句话记忆：`touch` = 建空文件 / 刷时间戳；不想误建就加 `-c`。

---

## 待办

- （暂无，今天学的是概念与命令，没有待跟进的事）

## 记笔记的规矩（给自己定的）

1. 涉及命令/代码行为，**先在 `/tmp` 实测再写**，不凭记忆下结论
2. 一天一个文件，文件名用日期 `YYYY-MM-DD.md`
3. 每天开头放「速览表」，一句话要点不能省
4. macOS 是 BSD 系命令，写的时候标注与 GNU/Linux 的差异
5. 概念类笔记要分清"能做什么"和"本质是什么"，不把比喻当定义
