# 2026-09-10 学习日记

## 今天学了什么（速览）

| 主题 | 一句话记住 | 分类 |
|---|---|---|
| **mv 命令** | 不搬数据只改门牌号，inode 全程不变；默认静默覆盖，务必加 `-i` | 命令行 |
| **WorkBuddy 工作空间** | 就是个文件夹，入口在输入框左下角；不选会生成时间戳目录 | 工具 |

## 今天的学习经历

- **22:16** 顺手开了个任务，因为没指定工作空间，系统自动生成了 `2026-09-10-22-16-01` 这种时间戳目录——这成了后来研究「工作空间怎么建」的直接起因。
- **22:29** 新建了 `learn` 工作区，打算正经把学到的东西记下来。
- **22:30** 记第一篇：mv 命令。先在 `/tmp` 里真跑了一遍，确认了 inode 不变、静默覆盖这些行为，才敢写进笔记。
- **22:38** 补第二篇：WorkBuddy 工作空间（查了官方文档）。
- **22:45** 觉得两篇散着不好看，把索引改成「一眼速览」，每篇配一句话要点。
- **22:47** 又做成网页版 `index.html`，能搜索、能筛选、待办能勾选。
- **22:49** 最终决定：一天一个文件，按日期命名，多余的散文件合并掉。

> 一条贯穿今天的经验：**记笔记之前先验证**。mv 的行为是跑出来的，都不是凭印象写的。

---

## 详细笔记

### 1. mv —— 移动与重命名

> 分类：命令行 / Linux & macOS

#### 一句话

`mv` = **move**，用来**移动**文件或目录，或者给它**改名**。底层是同一件事：改文件在目录树里的"位置 + 名字"，**文件内容本身一个字节都没动**。

#### 基本语法

```bash
mv [选项] 源 目标
```

#### 四种典型用法

```bash
mv report.txt 文档/          # 移动：把文件放进已存在的目录
mv report.txt 汇报.txt        # 重命名：目标不是目录时，就是改名
mv a.txt b.txt 文档/          # 多个源 → 目标必须是已存在的目录
mv old_dir/ new_dir/          # 目录也能移动或改名
```

#### 关键机制：inode 不变（实测）

在同一块磁盘/分区内，`mv` 只在目录项里改了个名字，**inode 编号不变**：

| 操作 | inode |
|---|---|
| 创建 `a.txt` | 9531614 |
| `mv a.txt d/`（移动） | 9531614 |
| `mv d/a.txt b.txt`（改名） | 9531614 |

**推论**：

- 移动一个 100GB 的大文件**瞬间完成**，数据块根本没挪。
- 权限、所有者、时间戳全部保留。
- 跨分区（硬盘 → U 盘）就不一样了：只能**复制 + 删除**，会慢，且可能丢权限/时间戳。

#### 最危险的默认行为：静默覆盖

```bash
echo "hello" > b.txt
echo "old"   > c.txt
mv b.txt c.txt        # 没有提示
cat c.txt             # → hello，原来的 "old" 没了，不可恢复
```

三个护栏选项（实测有效）：

| 选项 | 行为 | 实测结果 |
|---|---|---|
| `-i` | 覆盖前询问 | 输出 `overwrite exist/f.txt? (y/n [n])`，回车默认**不覆盖** |
| `-n` | 绝不覆盖 | 目标保留原内容，源文件**留在原地** |
| `-f` | 强制覆盖（默认就这样，主要用于抵消 `-i` 别名） | — |

**建议**：`alias mv='mv -i'` 写进 `~/.zshrc`。

#### 目录移动的易错点

目标目录**已存在** → 源被移入它内部；**不存在** → 视为重命名：

```bash
mv t exist/    # exist 已存在  → 变成 exist/t
mv t newname   # newname 不存在 → t 被改名为 newname
```

#### 常用选项速查

| 选项 | 作用 |
|---|---|
| `-i` | 覆盖前交互确认 |
| `-n` | 不覆盖已存在文件 |
| `-f` | 强制覆盖 |
| `-v` | 显示过程，如 `h.txt -> exist/h.txt` |
| `-u` | 仅当源比目标新才移动（GNU/Linux 有，macOS 无） |
| `-b` | 覆盖前先备份（GNU/Linux 有，macOS 无） |

⚠️ macOS 用 BSD 版 `mv`，**没有 `-u` 和 `-b`**，写跨平台脚本要注意。

#### mv vs cp

| | cp | mv |
|---|---|---|
| 结果 | 两份数据 | 只有一份 |
| inode | 新的 | 不变（同分区） |
| 速度 | 取决于文件大小 | 瞬间（同分区） |

#### 高频坑

1. **批量重命名做不到**，`mv *.txt *.md` 无效，`mv` 只接受最后一个参数是目标。用循环：
   ```bash
   for f in *.txt; do mv "$f" "${f%.txt}.md"; done
   ```
2. **变量必须加引号**，`mv $f dir/` 遇到带空格的文件名会炸。
3. **隐藏文件不会跟着走**，`mv * dir/` 不带走 `.gitignore`。

> 一句话记忆：`mv` 不搬数据，只改门牌号；搬得飞快，但撞了人也不吭声——记得加 `-i`。

#### 其他语境下的 MV

Music Video（音乐录像带）、Materialized View（物化视图）、Model View（MVC 的视图层）。

---

### 2. WorkBuddy 怎么新建工作空间

> 分类：工具 / WorkBuddy

#### 一句话

**工作空间 = 一个本地文件夹**，它是这个任务里所有文件读写的根目录。选了哪个目录，就在哪个目录里读文件、生成产物。

#### 两种建法

**方式一：显式指定（推荐）**

1. 看对话**输入框的左下角**
2. 点 **「选择工作空间」**
3. 选一个目录，或新建文件夹再选它
4. 之后的任务都基于这个目录读写

**方式二：不指定，用默认目录**

不点直接开始任务也行，会在默认目录生成结果。**代价是目录名变成时间戳**：

```
/Users/qinglin/WorkBuddy/
├── learn/                  ← 手动命名
├── 菜单2/                   ← 手动命名
├── Claw/
├── 2026-08-13-14-55-21/    ← 自动按时间戳生成
├── 2026-08-31-18-20-46/    ← 同上
└── 2026-09-10-22-16-01/    ← 同上（今天刚踩的）
```

时间戳目录的问题：**过一周就不知道里面是什么**。认真的项目都手动起名字。

#### 建完之后目录里会多出什么

```
你的工作区/
├── .workbuddy/            ← 项目级数据，不能删
│   └── memory/
│       ├── MEMORY.md      ← 长期项目笔记
│       └── 2026-09-10.md  ← 每日工作日志，只追加不覆盖
└── 你的业务文件/
```

#### 三层配置 / 记忆结构

| 层级 | 位置 | 作用范围 |
|---|---|---|
| 项目记忆 | `工作区/.workbuddy/memory/` | 只对这个工作区有效 |
| 用户全局 | `~/.workbuddy/MEMORY.md` | 跨项目，长期个人偏好 |
| 身份设定 | `~/.workbuddy/SOUL.md` `IDENTITY.md` `USER.md` | 我是谁、你是谁、怎么相处 |

其他全局文件：`~/.workbuddy/workspace-state.json`、`~/.workbuddy/mcp.json`（注意**不是** `.mcp.json`）。

#### 注意点

1. **开任务前先确认工作空间**，选错了文件就生成到别处去了。
2. **一个任务一个工作区**比较清爽。
3. **切换工作区 = 切换上下文**，之前那个项目的记忆不会自动跟过来。
4. 工作模式（输入框附近可切）：
   - **Craft**（你说我做）：直接动手，能读写文件、跑命令
   - **Plan**（先想后做）：先出方案，确认后再执行
   - **Ask**（只聊不动手）：只读，不改任何东西

> 参考：[创建任务 · WorkBuddy 文档](https://www.workbuddy.cn/docs/workbuddy/Create-Task)

---

## 待办

- （暂无，今天的笔记都是命令与工具，没有待跟进的事）

## 记笔记的规矩（给自己定的）

1. 涉及命令/代码行为，**先在 `/tmp` 实测再写**，不凭记忆下结论
2. 一天一个文件，文件名用日期 `YYYY-MM-DD.md`
3. 每天开头放「速览表」，一句话要点不能省
4. macOS 是 BSD 系命令，写的时候标注与 GNU/Linux 的差异
