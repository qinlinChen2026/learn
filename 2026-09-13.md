# 2026-09-13 学习日记

## 今天学了什么（速览）

| 主题 | 一句话记住 | 分类 |
|---|---|---|
| **Chrome「收藏目录」= 书签文件夹** | 书签栏就是收藏目录，右键「添加文件夹」就能建分组 | 工具 |
| **把两个网站收进去** | `⌘+D` 收藏，文件夹选「我的网站」；本地页用 `file://` 路径（或用本地服务器走 `http`） | 工具 |
| **.md 是什么（Markdown）** | 用纯文本写排版的轻量标记语言；今天写的 `2026-09-13.md` 就是它：`#`标题、`-`列表、`**粗**`加粗 | 概念 |
| **.html 是什么** | 网页的骨架，一堆 `<标签>` 告诉浏览器怎么排；网页版 `index.html` 就是它 | 概念 |

## 今天的学习经历

- **晚上** 想把两个本地网页（学习日记、家常菜单）收拾好，每天一点就开，不用每次找文件。
- 原来 Chrome 的**书签栏**就是现成的"收藏目录"：在书签栏上能建文件夹分组。
- 建了「我的网站」文件夹，把 `learn/index.html` 和 `菜单2/index.html` 两个本地页用 `file://` 地址收了进去。
- 顺手验证了另一条路：用 `python3 -m http.server` 起本地服务器再收藏 `http://127.0.0.1:端口`，两个站点都返回 200，这条路也通。
- **深夜** 把今天学到的东西记全，发现还差一块：搞懂了 **md 和 html 到底是什么**。
- md 一直就在眼前——今天写的 `2026-09-13.md` 就是 Markdown：**井号是标题、减号是列表、星号包起来是加粗**，不用排版工具栏也能把笔记写得有结构。
- html 则是另一回事：之前做的网页版 `index.html` 满屏 `<h1>` `<p>` `<a>` 这种尖括号标签，浏览器靠它们才知道哪是标题哪是段落。这一下串起来了——**我写的日记是 .md，看的网页版是 .html，正好是我今天学的两样东西**。

> 今天的体会：**书签栏 + 文件夹**把两个本地站收成了"我的网站"收纳盒；而 **.md 是给人/编辑器看的"源文件"，.html 是给浏览器看的"成品页"**——写笔记用 md，发上网就变成 html。

---

## 详细笔记

### 1. Chrome 的「收藏目录」就是书签文件夹

> 分类：工具 / 浏览器

#### 一句话

所谓"在收藏目录添加网站"，在谷歌浏览器里就是：**在书签栏建一个文件夹，把网页书签塞进去**。书签栏上的文件夹 = 收藏目录。

#### 基本操作（macOS）

| 操作 | 快捷键 / 入口 |
|---|---|
| 显示 / 隐藏书签栏 | `⌘ + Shift + B`，或 视图 → 总是显示书签栏 |
| 在书签栏建文件夹 | 书签栏**空白处右键 → 添加文件夹**，起名（如「我的网站」） |
| 收藏当前页 | `⌘ + D`，或地址栏右侧的☆ |
| 书签管理器（批量整理） | `⌘ + Option + B`，即 `chrome://bookmarks` |

> 书签栏默认可能是隐藏的，**先按 `⌘+Shift+B` 打开**才能看到它、往里塞文件夹。

---

### 2. 把两个网站加进去（实操）

> 分类：工具 / 浏览器

#### 两个网站在哪

| 网站 | 本地路径 | 收藏用的地址 |
|---|---|---|
| 学习日记 | `/Users/qinglin/WorkBuddy/learn/index.html` | `file:///Users/qinglin/WorkBuddy/learn/index.html` |
| 家常菜单 | `/Users/qinglin/WorkBuddy/菜单2/index.html` | `file:///Users/qinglin/WorkBuddy/菜单2/index.html` |

> `file://` 后面是**三个斜杠**（协议 `file:` + 空主机 + 绝对路径 `/Users/...`）。

#### 步骤（file:// 直收，最省事）

1. 书签栏右键「添加文件夹」→ 命名 **我的网站**。
2. 把上面「学习日记」那行地址粘进 Chrome 地址栏回车打开（或在 Finder 里把 `index.html` 拖进 Chrome 窗口）。
3. 按 `⌘+D` → 在弹窗的「文件夹」里选 **我的网站** → 名字改成 **学习日记** → 完成。
4. 家常菜单同理，`⌘+D` 收进「我的网站」，命名 **家常菜单**。
5. 之后点书签栏上的 **我的网站** 文件夹，两个网站一键直达。

#### 备选：起本地服务器，走 http（地址更稳）

本地静态站点可以直接用 Python 起服务，再收藏 `http` 地址：

```bash
# 学习日记
python3 -m http.server 8765 --bind 127.0.0.1 --directory /Users/qinglin/WorkBuddy/learn
# 家常菜单（另开一个终端 / 换端口）
python3 -m http.server 8766 --bind 127.0.0.1 --directory /Users/qinglin/WorkBuddy/菜单2
```

然后访问 `http://127.0.0.1:8765/index.html`、`http://127.0.0.1:8766/index.html`，再 `⌘+D` 收进「我的网站」。

**实测**：两个站点都返回 `200`，收藏 `http` 地址这条路完全可用。

| 方案 | 优点 | 缺点 |
|---|---|---|
| `file://` 直收 | 零配置，打开文件即收藏 | 地址带绝对路径；Chrome 同步对 `file://` 支持有限，换设备不一定跟过来 |
| 本地服务器 `http` | 地址固定、不受 `file://` 限制 | 每次要手动起 server（除非做成开机启动 / 部署） |

> 一句话记忆：书签栏就是收藏目录，右键建文件夹 → `⌘+D` 往里塞；想要固定地址就先起 `http.server`。

---

### 3. .md 文件就是 Markdown（你正在写的格式）

> 分类：概念 / 标记语言

#### 一句话

`.md` 是 **Markdown** 文件——一种"用纯文本写排版"的轻量标记语言。你今天写的 `2026-09-13.md` 就是 Markdown：**`#` 是标题、`-` 是列表、`**粗体**` 是加粗**，不用任何排版工具栏，纯键盘敲出来的字就能有结构。

#### 为什么好用

- 纯文本：记事本、VS Code、手机备忘录都能开，不挑软件
- 能直接渲染：GitHub、多数笔记软件、这个网页版都能把它变成带格式的页面
- 写文档/README/笔记的首选（你项目里的 `README.md` 也是 Markdown）

#### 看一眼就懂（今天日记里到处都是）

| 想表达 | Markdown 写法 | 渲染出来 |
|---|---|---|
| 大标题 | `# 2026-09-13 学习日记` | 大号标题 |
| 列表项 | `- 要点` | • 要点 |
| 加粗 | `**重点**` | **重点** |
| 表格 | 用 `|` 分隔（见本日记速览表） | 真表格 |

> 一句话记忆：`.md` = 用纯文本写排版的 Markdown；人写、好维护，是这套日记的"源文件"格式。

---

### 4. .html 是网页的骨架（网页版 index.html 就是它）

> 分类：概念 / Web

#### 一句话

`.html` 是 **HyperText Markup Language（超文本标记语言）**——网页的"骨架/积木"。浏览器靠它才知道页面长啥样。你打开的 `index.html`（网页版日记）就是 HTML 文件。

#### 本质：一堆带尖括号的标签

HTML 用 `<标签>内容</标签>` 这种"标签"告诉浏览器：这是标题、那是段落、这是个链接。

```html
<h1>学习日记</h1>
<p>这是一段文字</p>
<a href="2026-09-13.md">看原文</a>
```

> 上面这串，正是 `index.html` 里渲染你每一天日记的写法（`h1`=大标题、`p`=段落、`a`=超链接）。

#### 三个搭档（一句话分清角色）

| 技术 | 管什么 | 在 index.html 哪段 |
|---|---|---|
| **HTML** | 结构（哪是标题、哪是列表） | 那些 `<标签>` |
| **CSS** | 样式（颜色、间距、圆角） | 顶部 `<style>` 那一大段 |
| **JavaScript** | 交互（搜索、深浅色切换） | 底部 `<script>` 那两段 |

> 一句话记忆：`.html` = 给浏览器看的页面格式；HTML 搭结构、CSS 化妆、JS 让它能互动。

---

### 5. 两者的关系（今天最大的闭环）

> 分类：概念 / 串起来

你今天学的两个东西，正好是你这套笔记系统的**两种格式**：

- **日记 → 写 `.md`**（人写、好维护、能 git 版本管理）
- **网页版 → 是 `.html`**（浏览器看、能搜索筛选、能发上网）

实务里常是"**写 md，再转成 html 发布**"：本项目在线版（<https://qinlinchen2026.github.io/learn/>）就是把 md 生成的网页部署到 GitHub Pages 上（README 第 7 条提到的流程）。所以：

- `.md` 是**源文件**（你创作、改、存）
- `.html` 是**成品页面**（给别人/给浏览器看）

> 一句话记忆：**md 是给人/编辑器看的轻量文本，html 是给浏览器看的页面；写笔记用 md，发上网就变成 html。**

---

## 待办

- [ ] 搞清 `.md` 怎么转成 `.html` 并发布到在线版（pandoc / 静态站点生成器 / GitHub Pages），把"写 md → 发 html"这条链路自己走通一遍
- （书签栏收纳那块已做完，无待跟进）

## 记笔记的规矩（给自己定的）

1. 涉及命令/代码行为，**先在 `/tmp` 实测再写**，不凭记忆下结论（今天的 `http.server` 已实测 200）
2. 一天一个文件，文件名用日期 `YYYY-MM-DD.md`
3. 每天开头放「速览表」，一句话要点不能省
4. macOS 是 BSD 系命令，写的时候标注与 GNU/Linux 的差异
5. 浏览器 UI 操作类笔记，步骤要写清快捷键和菜单入口
